includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 8
    # PHP does not enforce array element types; keep runtime boundary validation.
    treatPhpDocTypesAsCertain: false
    paths:
        - src
    tmpDir: .phpstan.cache
